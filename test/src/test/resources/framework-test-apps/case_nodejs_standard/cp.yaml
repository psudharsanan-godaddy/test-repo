app:
  frameworkType: nodejs

deployment:
  volumes:
    fsGroup:
      enabled: true
    writableVol:
      enabled: true
  readOnlyRootFilesystem: false

configs:
  standard:
    db:
      enabled: false
    log:
      enabled: false
