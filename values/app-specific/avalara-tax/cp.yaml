app:
  frameworkType: spring-boot
  size: LARGE
  tmpdir: /tmp/tomcat

configs:
  standard:
    auth:
      enabled: false
    db:
      enabled: false
    log:
      enabled: false
  springBoot:
    enabled: true
    application:
      enabled: true
      logging:
        type: CONSOLE_JSON
        logbook:
          enabled: true

deployment:
  readinessProbe:
    initialDelaySeconds: 30
  livenessProbe:
    enabled: true
    initialDelaySeconds: 45
