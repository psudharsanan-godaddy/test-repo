app:
  frameworkType: nodejs

configs:
  standard:
    auth:
      enabled: false
    db:
      enabled: false
    log:
      enabled: false
    prometheus:
      enabled: false
    sensitive:
      defaultMode: 420

deployment:
  readOnlyRootFilesystem: false
  resources:
    requests:
      cpu: 25m
