app:
  frameworkType: nodejs

configs:
  standard:
    auth:
      enabled: false
    db:
      enabled: false
    log:
      enabled: false
    prometheus:
      enabled: false

deployment:
  resources:
    requests:
      cpu: 25m
