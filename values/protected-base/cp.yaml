deployment:
  extraVolumeMounts: {}
  extraVolumes: {}
  readinessProbe:
    enabled: true

configs:
  mountPath: /tmp
  standard:
    enabled: true
    sensitive:
      defaultMode: 0400
    crypto:
      kmsKeyArns: {}
